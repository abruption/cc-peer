name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: test (${{ matrix.os }}, python ${{ matrix.python }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # 3.9 is the floor the README promises; macOS is half the fleet.
        os: [ubuntu-latest, macos-latest]
        python: ['3.9', '3.13']
        include:
          - os: windows-latest
            python: '3.13'
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python }}
      - name: Compile
        run: python -m compileall -q cc_peer.py test_cc_peer.py
      - name: Unit tests
        run: python -m unittest test_cc_peer -v

  shell:
    name: shellcheck install.sh
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: shellcheck
        # SC2016 (no expansion in single quotes) is the point: the heredoc we
        # pipe to ssh must reach the remote shell with $HOME intact.
        # SC1007 misreads `CDPATH= cd -- ...`, which is the standard guard
        # against cd printing a path, not an empty assignment.
        run: shellcheck --shell=sh --exclude=SC2016,SC1007 install.sh

  install-smoke:
    name: install smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Fresh install
        run: ./install.sh
      - name: Verify installation
        run: |
          test -f "$HOME/.claude/skills/cc-peer/cc_peer.py"
          test -L "$HOME/.local/bin/cc-peer"
          "$HOME/.local/bin/cc-peer" --version
      - name: Re-run upgrades existing install
        run: |
          sed -i 's/__version__ = .*/__version__ = "0.0.1"/' "$HOME/.claude/skills/cc-peer/cc_peer.py"
          ./install.sh
          version=$("$HOME/.local/bin/cc-peer" --version)
          echo "$version"
          echo "$version" | grep -qv "0.0.1"
      - name: Uninstall
        run: |
          ./install.sh --uninstall
          test ! -d "$HOME/.claude/skills/cc-peer"
          test ! -L "$HOME/.local/bin/cc-peer"
